# Robotic-Failure-Detection-Datasets-Benchmark
Awesome list of Robotic Failure Detection Datasets. They can serve as training data for failure detection models, or reward models, as well as benchmarks to evaluate in a systematic and reproducible way failure detection methods.
